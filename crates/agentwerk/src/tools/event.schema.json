{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Event identifier. Lowercase snake case is conventional; every custom or built-in name is accepted."
    },
    "data": {
      "description": "Details reported with the event. Omit it for an empty object. When `name` is `task_finished`, put the completion arguments shown for this call inside `data`."
    }
  },
  "required": [
    "name"
  ],
  "allOf": [
    {
      "if": {
        "required": ["name"],
        "properties": { "name": { "const": "task_finished" } }
      },
      "then": {
        "required": ["data"],
        "properties": {
          "data": {}
        }
      }
    }
  ],
  "examples": [
    {
      "name": "...",
      "data": {}
    },
    {
      "name": "task_finished",
      "data": {}
    }
  ]
}
