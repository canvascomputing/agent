{
  "type": "object",
  "description": "Finish the current task.",
  "properties": {
    "result": {
      "description": "Final answer as a native JSON value. Omit only when there is nothing to report."
    }
  },
  "additionalProperties": false,
  "examples": [
    { "result": "..." }
  ]
}
